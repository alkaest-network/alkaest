# Open Questions

- Should issue claiming be exclusive, competitive, or hybrid?
- How should validator membership be declared in project integration?
- What is the first settlement model: manual, signed record, or escrow-ready?
- Which parts of GitHub integration belong in RFC-0003 vs a separate integration RFC?
- How should donated funds be allocated across `alkaest-network` issues?
- Who decides which Alkaest issues are funding candidates on the network?
- Should a `WorkItem` be allowed to exist before funding, or should MVP require funding first?
- Should protocol-visible hashes and keys be rendered as hex, base64url, or different formats by type?
- Is lexicographic `eventId` tie-breaking sufficient for MVP conflict resolution, or should some event classes carry stronger authority sooner?
